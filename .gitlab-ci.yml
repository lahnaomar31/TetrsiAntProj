stages:
  - build
  - test

build_job:
  stage: build
  script:
    - docker build -t my-tetris-app .

test_job:
  stage: test
  script:
    - docker run --rm my-tetris-app
  artifacts:
    reports:
      junit: bin/test-results/TEST-*.xml
    paths:
      - bin/test-results/
